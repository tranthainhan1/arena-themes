import AT_main from "./_arn";

let IconsBox = {
  onLoad: function () {
    this.tns = AT_main.initTinySlider(this.container);
  },
};

let FeaturedCollection = {
  onLoad: function () {
    this.tns = AT_main.initTinySlider(this.container);
  },
};

let LogoList = {
  onLoad: function () {
    this.tns = AT_main.initTinySlider(this.container);
  },
};

export { IconsBox, FeaturedCollection, LogoList };
